import { WorkflowsView } from "@/app/console/workflows/workflows-view";

/** WorkflowsPage shows only the tenant template menu. / WorkflowsPage 仅展示租户模板菜单。 */
export default function WorkflowsPage() { return <WorkflowsView operator={false} />; }
