# P10 验收记录 · 2026-09-12

P10 已启动，尚未整体完成。本轮完成验收工具补强、Console Q03、短时长稳冒烟与同版逐副本替换。24h 长稳已另行启动，最终结果仍需核验；真实流量熔断校准尚未执行，默认值没有调整。复现步骤与判定规则见 [验收手册](../../../deploy/p10-acceptance.md)。

环境为 darwin/arm64、Go 1.27.1、Node 24.21.0、Chrome 153.0.8010.37、Playwright 1.63.0。工作区变更未提交；[源码校验和](source-checksums.json) 记录本轮服务文件及其基准修订。这里只记录实测范围，不扩大为生产容量、SLO 或跨版本兼容承诺。

## Console Q03

最终浏览器运行始于 `2026-09-12T09:42:59.545Z`，75 项通过，0 跳过、0 失败、0 重试通过，耗时 41.04s。完整计数与内存数据见 [console-summary.json](console-summary.json)。

- 22 个已登录页面分别检查 1440、390、320px 布局与标签，包含已加载的路由/模板编辑器及他人账户管理控件。
- 两个窄屏登录页、读失败恢复、创建失败重试、Tab 焦点圈定、Escape/关闭后的焦点恢复、一次性 Key 清理、390×480 短屏弹窗、当前导航标记。
- 长名称与标识符使用有上限的合成内容；节点标签同时检查卡片内部裁切，避免只有文档宽度正常就误判通过。
- 审计与请求检索各连续翻阅 10,000 条记录，单页 200 条，共 50 页；每页 DOM 数据行均少于 55，末页与键盘滚动通过，审计筛选回到第 1 页。

回收后 JS 堆（字节）：

| 页面 | 第 5 页 | 第 25 页 | 第 50 页 | 5→50 页增长 | 三次采样的 DOM 数据行 |
| --- | ---: | ---: | ---: | ---: | --- |
| 租户审计 | 6,719,064 | 7,106,232 | 7,262,956 | 543,892（约 0.52 MiB） | 25 / 25 / 25 |
| 平台请求检索 | 6,694,236 | 7,039,496 | 7,270,308 | 576,072（约 0.55 MiB） | 25 / 25 / 25 |

两者低于此次分页回归门槛 8 MiB。该测试不证明任意时长、任意负载下不存在内存增长。

复现并修复的问题：长名称撑宽表格/网格、租户页头长文本溢出、节点标签被卡片裁切、长模板 ID 按钮溢出、短屏创建用户弹窗顶部超出视口、运维创建/筛选和用户密码重置控件缺少标签，以及运行历史页同时标记三个当前导航项。对话框焦点圈定与恢复的既有行为通过了真实键盘检查。

API 和会话使用隔离样本，未调用真实 ControlPlane；这是 Chrome 的上述页面与代表性交互验收，不是所有浏览器/辅助技术/业务弹窗或后端授权流程的全量验收。截图、trace 和完整 Playwright JSON 保存在本机 `dist/p10/2026-09-12/browser/` 与 `console-results.json`。

## 2m 长稳工具冒烟

原始证据：[CSV](soak-2m.csv)、[摘要](soak-2m.summary.json)、[测试日志](soak-2m.log)。`-race` 运行退出 0，CSV 与 JSON 的样本数量和范围已交叉核对。

| 指标 | 实测 |
| --- | --- |
| 观测窗口 | 120.001s，25 个样本，5s 间隔 |
| 流量 | 1,875 请求，5 失败，失败率 0.267% |
| 协程 | 基线 82，末值 82 |
| GC 后堆 | 基线 1,736,860 B，末值 1,883,824 B（+8.46%） |
| 连接 | 每次采样为 3 |
| 推理空闲槽合计 | 4–6，允许范围 1–12 |
| 最大墙钟采样间隔 | 5.003s，允许上限 10s |

请求计数包含最多 5s 预热；观测窗口不包含预热。5 次请求失败没有进一步归因，本次仅确认它们低于既有 1% 失败率门槛，不把这份记录称为零错误运行。测试使用单进程机群与固定响应合成后端。

## 同版逐副本替换

[直接捕获的五次日志](rolling.log) 对应 `-race -count=5`，进程退出 0，耗时 13.799s。每次都完成三个副本的停机窗口探测，每个后台监视周期都能找到一个可服务副本。

| 次数 | 成功 / 尝试 | 最长连续成功间隔 |
| --- | --- | --- |
| 1 | 140 / 141 | 10.077209ms |
| 2 | 203 / 204 | 10.213500ms |
| 3 | 410 / 412 | 10.060458ms |
| 4 | 455 / 456 | 10.549750ms |
| 5 | 374 / 375 | 10.893667ms |

这些是当前源码的单进程副本替换数据。旧/新版本混跑、发版、Agent 升级与数据库迁移没有纳入本轮；版本与发版安排由维护者处理。

## 24h 启动记录与剩余验收

冻结的 `-race` 测试二进制于 `2026-09-12T09:41:33Z`（台北 17:41:33）启动。启动时已看到第一个有效样本：连接 3、推理空闲槽 4、协程 83；这不是 24h 完成证据。

本机目录为 `dist/p10/2026-09-12/soak-24h/`。`manifest.json` 保存二进制与测试源码校验和、时长与来源，`process.json` 保存本次进程身份。运行写入 `soak.csv`，结束后核对 `soak.csv.summary.json`、`go-test.log`、`exit-code.txt` 和 `finished-at.txt`。监督进程 PID 为 21929，测试 PID 为 21930；PID 仅属于本次启动记录，不作为以后仍在运行的证据。

完成后须按手册核对满 24h、采样连续性、阈值、退出 0 与无 race/泄漏错误，再归档完整结果。休眠、暂停、退出或测试失败均须按实际结果记录；当前 P10 保持未勾选。

真实流量校准仍缺少本次可使用的后端/模型环境；本机 Ollama 在检查时未运行。未凭合成流量修改连续失败 5 次、初始冷却 5s、最大冷却 2m 的默认值。

## 质量门禁

`gofmt -l ./service ./api` 无输出，`go vet ./...`、`go build ./...`、`go generate ./api/...`（生成文件无 diff）、`go test ./...`、`go test -race ./service/...` 全部通过。Console lint、TS7 typecheck、149 个 Node 单元测试、Webpack 生产构建与 75 个浏览器用例通过。独立审查的路由就绪等待与节点标签裁切问题均已补测并修复，无剩余审查项。

完整门禁日志保存在本机 `dist/p10/2026-09-12/`；可复现的工具与轻量实测数据随本记录保留。此次默认测试未接入真实数据库、GPU 或推理后端。
